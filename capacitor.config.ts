import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Huecas de comida',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
